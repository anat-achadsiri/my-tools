<Window x:Class="SoftSnapWPF.RegionSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True"
        WindowState="Maximized" Cursor="Cross"
        PreviewKeyDown="Window_KeyDown"
        Focusable="True">

    <Grid x:Name="RootGrid"
          MouseLeftButtonDown="Canvas_MouseDown"
          MouseMove="Canvas_MouseMove"
          MouseLeftButtonUp="Canvas_MouseUp">

        <!-- Full screenshot (clear, 100% opacity) -->
        <Image x:Name="BgImage" Stretch="Fill"/>

        <!-- Dark overlay (dims unselected area) -->
        <Grid x:Name="DimOverlay" IsHitTestVisible="False">
            <!-- 4 rectangles around selection to dim -->
            <Rectangle x:Name="DimTop" Fill="#99000000"/>
            <Rectangle x:Name="DimBottom" Fill="#99000000"/>
            <Rectangle x:Name="DimLeft" Fill="#99000000"/>
            <Rectangle x:Name="DimRight" Fill="#99000000"/>
        </Grid>

        <!-- Initial full dim (before drag starts) -->
        <Rectangle x:Name="FullDim" Fill="#66000000" IsHitTestVisible="False"/>

        <!-- Selection border -->
        <Border x:Name="SelectionBorder" Visibility="Collapsed" IsHitTestVisible="False"
                BorderBrush="#6366F1" BorderThickness="2">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" BlurRadius="6" Opacity="0.6" Color="#6366F1"/>
            </Border.Effect>
        </Border>

        <!-- Size label -->
        <Border x:Name="SizeLabel" Visibility="Collapsed" IsHitTestVisible="False"
                Background="#CC1E1B4B" CornerRadius="4" Padding="8,3"
                HorizontalAlignment="Left" VerticalAlignment="Top">
            <TextBlock x:Name="SizeText" Foreground="White"
                       FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold"/>
        </Border>

        <!-- Instruction text (centered) -->
        <Border x:Name="HintBorder" IsHitTestVisible="False"
                Background="#CC1E1B4B" CornerRadius="8" Padding="20,10"
                HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,40,0,0">
            <TextBlock Text="&#x2702;  ลากเมาส์เลือกพื้นที่   |   ESC = ยกเลิก"
                       Foreground="White" FontFamily="Segoe UI" FontSize="15" FontWeight="SemiBold"/>
        </Border>
    </Grid>
</Window>
